version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./pratt.db:/app/pratt.db
    environment:
      - DATABASE_URL=sqlite:///./pratt.db
      - DEBUG=True
    restart: unless-stopped
